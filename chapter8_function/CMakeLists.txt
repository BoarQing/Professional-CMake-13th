cmake_minimum_required(VERSION 3.22.1)
project(project_function
        VERSION 1.0.0
        LANGUAGES C CXX)

option(baseFuncCMake "if compile" FALSE)
if(baseFuncCMake)
        include(base_func.cmake)
endif()
option(argCMake "if compile" FALSE)
if(argCMake)
        include(arg.cmake)
endif()
option(keywordCMake "if compile" FALSE)
if(keywordCMake)
        include(keyword.cmake)
endif()
option(scopeCMake "if compile" FALSE)
if(scopeCMake) 
        include(scope.cmake)
endif()
option(overrideCMake "if compile" FALSE)
if(overrideCMake)
        include(override.cmake)
endif()
option(specialVarCMake "if compile" FALSE)
if(specialVarCMake)
        include(special_var.cmake)
endif()
option(invokeCMake "if compile" FALSE)
if(invokeCMake)
        include(invoke.cmake)
endif()
